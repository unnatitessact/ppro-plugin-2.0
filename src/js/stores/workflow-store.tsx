// import { Edge } from "@xyflow/react";
// import { create } from "zustand";

// const useWorkflowStore = create<{
//   selectedNode: string | null;
//   setSelectedNode: (selectedNode: string | null) => void;
//   showCommentsPanel: boolean;
//   toggleCommentsPanel: () => void;
//   selectedEdge: Edge | null;
//   setSelectedEdge: (selectedEdge: Edge | null) => void;
// }>((set) => ({
//   selectedNode: null,
//   setSelectedNode: (selectedNode: string | null) => set({ selectedNode }),
//   showCommentsPanel: false,
//   toggleCommentsPanel: () =>
//     set((state) => ({ showCommentsPanel: !state.showCommentsPanel })),
//   selectedEdge: null,
//   setSelectedEdge: (selectedEdge: Edge | null) => set({ selectedEdge }),
// }));

// export default useWorkflowStore;
